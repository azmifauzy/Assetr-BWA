name: assetr
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  get: ^4.6.5
  shared_preferences: ^2.0.15
  http: ^0.13.4
  d_method: ^0.0.2
  intl: ^0.17.0
  d_view: ^0.0.3
  d_info: ^0.0.7
  d_chart: ^2.2.11
  d_input: ^0.0.2
  lottie: 2.2.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/
